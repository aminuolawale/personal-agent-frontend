# Personal Agent Frontend

Next.js UI for the recursive personal planner agent.

## Setup

```bash
npm install
cp .env.example .env.local
```

Set `NEXT_PUBLIC_API_BASE_URL` to the deployed or local backend URL.

```bash
npm run dev
```

The frontend runs at `http://localhost:3000`.

## Deploy

```bash
vercel --prod
```

